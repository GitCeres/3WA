<nav id="nav-main">
    <div class="main-navlinks">
        <button class="hamburger" type="button">
            <span></span>
            <span></span>
            <span></span>
        </button>

        <div class="navlinks-container">
            <ul class="navlinks-container-slide">
                <li>
                    <a href="{{ path('homepage') }}">Accueil</a>
                </li>
                <li>
                    <a href="{{ path('app_escape_game') }}">Escape Game</a>
                </li>
                <li>
                    <a href="{{ path('app_films') }}">Films</a>
                </li>
                <li>
                    <a href="#">Encore</a>
                </li>
                <li>
                    <a href="#">De nouveau</a>
                </li>
                <li>
                    <a href="#">Voilà</a>
                </li>
            </ul>
        </div>
    </div>

    <a href="{{ path('homepage') }}" class="nav-icon">Le Récap</a>

    <div class="nav-authentication">
        <a href="{{ path('app_login') }}" class="login-mobile">
            <i class="fa-solid fa-user"></i>
        </a>

        <div class="sign-btn">
            {% if app.user %}
                <a href="{{ path('app_logout') }}" class="btn-sign">Déconnexion</a>
                <a href="{{ path('app_account') }}" class="btn-sign">Compte</a>
            {% else %}
                <a href="{{ path('app_login') }}" class="btn-sign">S'identifier</a>
                <a href="{{ path('app_register') }}" class="btn-sign">S'enregistrer</a>
            {% endif %}
        </div>
    </div>
</nav>