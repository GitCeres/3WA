{% extends "admin/base.html.twig" %}

{% block title %}Édition des données de {{ user.fullName }}
{% endblock %}

{% block body %}
	<h1>Édition des données de {{ user.fullName }}</h1>

    <div class="back-office">
		{% include "admin/shared/_aside.html.twig" %}
        <div class="back-office-user-edit">
            {{ form_start(form) }}

			{{ form_widget(form) }}

			<button type="submit" class="btn btn-primary btn-center">
                <i class="fas fa-save"></i>
                Mettre à jour
            </button>

			{{ form_end(form) }}
        </div>
	</div>
{% endblock %}